<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.mad_unhinged.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" />

    <activity
        android:id="@+id/matchesActivity"
        android:name="com.example.mad_unhinged.MatchesActivity"
        android:label="activity_matches"
        tools:layout="@layout/activity_matches" />

    <activity
        android:id="@+id/chatListActivity"
        android:name="com.example.mad_unhinged.ChatListActivity"
        android:label="activity_chat_list"
        tools:layout="@layout/activity_chat_list" />

</navigation>